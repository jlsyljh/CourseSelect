
<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <div class="panel panel-info">
        <div class="panel-body">
            <% if $XUANKE %>
                <%= link_to "关闭选课", main_app.xuanke_course_path(current_user.courses), class: "btn btn-primary btn-block" %>
            <% else %>
                <%= link_to "开放选课", main_app.xuanke_course_path(current_user.courses), class: "btn btn-primary btn-block" %>
            <% end %>
        </div>
    </div>
  </div>
</div>